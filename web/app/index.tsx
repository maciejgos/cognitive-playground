/*
    Create UI with:
    1. Search bar on top
    2. Filters menu on left sidebar
    3. Results center on screen
    4. After click result, show details in modal window
    5. Modal window should contain all details, close button and 5 predefined buttons on right side
*/

export default function Home() {
    return (
        <div>
            <h1>Home</h1>
        </div>
    )
}